# GSS_QR

Streamlit app for generating QR codes for Good Shepherd Catholic School.

## Features

- Generate QR codes from a URL
- Use school colors or custom colors
- Optionally embed an uploaded logo in the center
- Log generated QR codes to a local SQLite history database

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```
